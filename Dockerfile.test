FROM node:22-slim

WORKDIR /app

# Copy package files first for layer caching
COPY package*.json ./

# Install all deps (including devDependencies for tests)
RUN npm ci

# Copy source
COPY . .

# Run vitest in CI mode — exits with code 0/1
CMD ["npm", "test", "--", "--run"]
