# EditorConfig — https://editorconfig.org
# Enforces consistent encoding and line endings across all editors.

root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

# SQL migration files: explicitly no BOM
[*.sql]
charset = utf-8
