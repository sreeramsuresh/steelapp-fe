# Mock Data Mode Configuration
# Use this file to run the app with mock data (no database needed)
# Start with: npm run dev (and set NODE_ENV to use this file)

# Enable mock data mode
VITE_USE_MOCK_DATA=true

# Mock data scenario (normal | empty | heavy | edge)
VITE_MOCK_SCENARIO=normal

# Simulate network delay in milliseconds
VITE_MOCK_DELAY=500

# API URL (not used in mock mode, but required for build)
VITE_API_URL=http://localhost:3000
